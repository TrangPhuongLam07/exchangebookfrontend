import { AppBar, Typography, Button } from "@mui/material";

const Header = () => {
  return (
    <AppBar>
      <Typography />
      <Button>Login</Button>
    </AppBar>
  );
};

export default Header;
