import { AppBar, Typography, Button, Toolbar } from "@mui/material";

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
      <Typography />
      </Toolbar>
    </AppBar>
  );
};

export default Header;
