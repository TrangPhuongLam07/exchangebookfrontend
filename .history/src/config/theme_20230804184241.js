import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#233044",
      light: "skyblue",
    },
    secondary: {
      main: "#15C630",
    },
    white: "#fff",
  },
});
export default theme;
