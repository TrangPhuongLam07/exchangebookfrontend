import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#1760A5",
      light: "skyblue",
    },
    secondary: {
      main: "#15C630",
    },
  },
});
export default theme;
