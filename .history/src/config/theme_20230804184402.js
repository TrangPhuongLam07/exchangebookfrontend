import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#233044",
      light: "skyblue",
      white: "#fff",
    },
    secondary: {
      main: "#15C630",
    },
  },
});
export default theme;
