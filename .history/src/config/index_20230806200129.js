import theme from "./theme";
import { publicRoutes, privateRoutes } from "./routes";
import { default as sizes } from "./size";
import { default as colors } from "./color";
const config = {
  routes,
  theme,
  colors,
  sizes,
};

export default config;
